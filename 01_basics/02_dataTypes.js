"use strict"; // Treat all code as newer version

//alert(3+3) // It'll give u error as we are using node not browser's engine to run our js file.

console.log(9+6) //code readability should be high

console.log("Sameer")

// PRIMITIVE DATATYPES IN JS
let name = "Sameer" // String
let age = 21        // number
let isLoggedIn = true //Boolean
let state // if left like this It'll give undefined
let city = null // to define null value we have to initialise with null

// number >= 2 to power 53
// bigint for greater values
// string = "" i.e. empty string
// boolean = True / False
// null => standalone value
// undefined = value is not assigned yet
// symbol => when we have to work with UNIQUE values

// Typeof is used to find the details of any data structure.

console.log(typeof("Hitesh"))

//OBJECT
console.log(typeof undefined); //undefined
console.log(typeof null) //object